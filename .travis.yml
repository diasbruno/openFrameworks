language: cpp

compiler:
    - gcc

script: 
    - cd tests && sudo ./compile_OF.sh && sudo ./tester -r -nb

branches:
    only:
        - tests

notifications:
    email:
        recipients:
            - dias.h.bruno@gmail.com
        on_success: change
        on_failure: always
