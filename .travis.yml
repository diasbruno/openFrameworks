language: cpp

compiler:
    - gcc

script: 
    - cd tests && sudo ./compile_OF.sh && gem install rake && ./tester -r

branches:
    only:
        - tests

notifications:
    email:
        recipients:
            - dias.h.bruno@gmail.com
        on_success: change
        on_failure: always
